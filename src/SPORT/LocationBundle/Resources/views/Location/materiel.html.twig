{# empty Twig template #}
{# empty Twig template #}


{% extends "SPORTLocationBundle::layout.html.twig" %} 


{% block stylesheets %}
    {{ parent() }}
{% endblock %}


{% block content %}

    {{ parent() }}

    <br/>
    <br/>
    <div class="container">

        <div class="row">

            <div class="col-lg-3">
                <br/><br/>
                <div class="list-group">
                    <a href="#" class="list-group-item active">Catégorie</a>
                    {% for item in category %}
                        <a href="{{ path('sport_location_materielpage', {'id':item.id}) }}" class="list-group-item">{{item.CategoryNom }}</a>
                    {% endfor %}
                </div>
            </div>
            <br/><br/><br/>   <div class="col-xs-5 col-sm-6 col-lg-4"><a href="{{path('sport_location_panier')}}">
                    <span value="1" class="glyphicon glyphicon-shopping-cart">{{panier|length}}</span>
                </a></div>
        </div>

    </div>


                <div class="bg-4">
        <!-- /.row -->
        <div class="container">   

            <!-- /.row -->
            <div class="row ">
                <!-- Page Features -->
                {% for item in materiel %}


                     <div class="col-sm-4 col-xs-6">
                        <div class="panel panel-default">
                            <div class="panel-thumbnail">
                                <img src="{{ asset(item.image.WebPath) }}"
                                    alt="{{ item.image.alt }}" width="600" height="400"  class="img-responsive">
                            </div>
                            <div class="panel-body">
                                <h3>{{ item.MaterielNom }}</h3>
                                 <p><h4>{{item.MaterielPrixlocation|tva(item.TvaPrix())}}€</h4></p>
                                <p>
                                    {% if panier[item.id] is not defined %}
                                        <a href="{{path('sport_location_ajouterpage',{'id':item.id})}}" class="btn btn-primary">Ajouter!</a> {% endif %} <a href="{{path('sport_location_infopage',{'id':item.id})}}" class="btn btn-default">Plus de Détails!</a>
                                    </p>
                                </div>
                            </div>
                        </div>
                        {% endfor   %}
                            <!-- row-->
                        </div>

                        </div>
                    </div>
                            
                   <div class="row">

                            <div class="col-md-offset-4 col-md-6 ">
                                <div class="navigation">
                                    {{ knp_pagination_render(materiel) }}
                                </div>
                            </div>
                        </div>
                    {% endblock %}

                        {% block javascripts %} {{ parent() }}{% endblock %}